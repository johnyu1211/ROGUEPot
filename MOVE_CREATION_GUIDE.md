# 🎮 ROGUEPot 기술 애니메이션 제작 메뉴얼 & AI 개발 지침서

> **[필독]** 이 문서는 ROGUEPot 배틀 기술 애니메이션(Move Animation)을 구현하는 모든 AI 에이전트와 개발자를 위한 **표준 작업 규칙서**입니다.
> 작업을 시작하기 전에 반드시 이 문서를 정독하고 지침을 100% 준수해야 합니다.

---

## 🚨 1. AI 절대 금지 규칙 (사고 방지 수칙)

#### ❌ 절대 금지 1: 프레임별 이미지 추출 및 AI 자가 검증 루프
- **금지 행위**: `extractFrames.ts`를 실행하여 프레임별 PNG 이미지를 일일이 추출하고, AI가 도구를 호출해 이미지를 하나씩 읽어가며 셀프 검증하는 행위.
- **이유**: 엄청난 토큰 낭비, 컨텍스트 소진, 작업 지연 및 무한 루프 유발.
- **올바른 방식**:
  1. 코드 작성/수정
  2. `npm run build` (컴파일 검증)
  3. `npx tsx scripts/testBattleMove.ts <move-key>` (GIF 생성 검증)
  4. `curl.exe -s -X POST http://localhost:3456/api/clear-cache` (웹 뷰어 캐시 갱신)
  5. 즉시 사용자에게 작업 완료 및 웹 뷰어 확인 요청 보고!
  *(시각적 검증은 유저가 웹 뷰어 http://localhost:3456에서 직접 눈으로 확인합니다.)*

### ❌ 절대 금지 2: 시전자/피격자 좌표 오프셋 동기화 실수
- **자주 발생하는 사고**: 시전자가 회전 댄스나 도약을 할 때, 삼항 연산자 실수로 인해 **수비자(상대방)도 같이 돌거나 움직이는 버그**.
- **원인 예시 (잘못된 코드)**:
  ```typescript
  // ⚠️ 나쁜 예: isP가 true일 때 eOffset에도 { 8, 3 }이 들어가서 상대도 같이 움직임!
  pOffset: isP ? { x: 8, y: 3 } : { x: -8, y: -3 },
  eOffset: !isP ? { x: -8, y: -3 } : { x: 8, y: 3 },
  ```
- **해결책 (반드시 이 헬퍼 함수 패턴 사용)**:
  ```typescript
  // ✅ 올바른 예: 시전자만 움직이고, 수비자는 완벽하게 { 0, 0 } 고정!
  const cOff = (x: number, y: number) => ({
    pOffset: isP ? { x, y } : { x: 0, y: 0 },
    eOffset: !isP ? { x: -x, y: -y } : { x: 0, y: 0 },
  });

  // 프레임 안에서 사용:
  {
    ...baseFrame,
    ...cOff(22, -10), // 시전자만 전방 도약!
  }
  ```

### ❌ 절대 금지 3: 스프라이트 임의 반전/전면화
- 사용자가 명시적으로 요구하지 않는 한 `usePlayerFront` 등을 임의로 켜지 마세요.
- 플레이어 포켓몬은 기본적으로 **후면(Back)** 스프라이트 상태를 유지하며, `pOffset`의 위치 이동 및 `pScale` 탄성으로 모션을 표현합니다.

---

## 📂 2. 파일 아키텍처 & 4개 묶음 원칙

기술 애니메이션은 **정확히 4개 단위**로 묶어서 관리합니다.

```
src/
├── renderers/moves/gen1/
│   ├── move001_004.ts
│   ├── move005_008.ts
│   ├── ...
│   ├── move077_080.ts   <-- (독가루, 저리가루, 수면가루, 꽃잎댄스)
│   └── move081_084.ts   <-- 다음 4개 묶음
├── battle/moves/
│   ├── definitions/
│   │   ├── 077_poison_powder.ts
│   │   ├── 078_stun_spore.ts
│   │   ├── 079_sleep_powder.ts
│   │   └── 080_petal_dance.ts   <-- 1기술당 1파일
│   └── moveRegistry.ts           <-- 기술 등록 레지스트리
viewer/
├── movesData.ts                  <-- 웹 뷰어 메타데이터 및 4개 그룹 표시
```

### 작업 4단계 체크리스트:
1. **렌더러 함수 작성**: `src/renderers/moves/gen1/moveNNN_NNN.ts` 끝에 `draw<MoveName>Effect` 구현 및 export.
2. **정의 파일 생성**: `src/battle/moves/definitions/NNN_<name>.ts` 생성 (`buildFrames` + `drawEffect`).
3. **레지스트리 등록**: `src/battle/moves/moveRegistry.ts`에 import 및 키 등록 (`key`, `id`, `한글명`, `NNN`).
4. **뷰어 등록**: `viewer/movesData.ts`의 `HANDCRAFTED_MAP`에 추가 (`status: "complete"`, `isVerified: false`).

---

## 🎨 3. 연출 및 그래픽 디자인 노하우

### 1) 256색 팔레트 최적화 (Octree Optimizer)
- GIF 인코더는 256색 팔레트를 사용하므로, 무수한 색상의 미세한 그라디언트 난사를 피하세요.
- **핵심 고채도 비비드 단색 3~4단계**로 대비를 주어야 도트가 뭉개지지 않고 선명하게 출력됩니다:
  - 딥 톤 (외곽/음영)
  - 베이스 톤 (몸체)
  - 하이라이트/코어 톤 (중심 광택, #FFFFFF 등)

### 2) 타격 임팩트 3종 세트
- **1프레임 백색 섬광**: 타격 순간 hitFlash: true (1프레임 번쩍임으로 타격감 전달).
- **다이아몬드 섬광 & 충격파 링**: 피격 지점 중심에 다이아몬드 십자 스파크 + 타원형 충격파 링 확산.
- **피격자 흔들림 (Recoil)**: 타격 순간 피격 포켓몬만 좌우/상하로 흔들림 (eOffset / pOffset).

### 3) 시전자 모션 기승전결 (Choreography)
- **1단계 (모으기/시동, 70~90ms)**: 시전자가 뒤로 살짝 물러나거나, 웅크려 힘 축적 (`scale: 0.9, 1.1`).
- **2단계 (돌진/방출, 90~120ms)**: 대상 방향으로 과감하게 도약 돌진 (비거리 50~75px 권장, `scale: 1.2, 0.8`).
- **3단계 (타격 & 쇄도, 110~140ms)**: 이펙트가 상대를 맹타하며 피격자 리액션 작렬.
- **4단계 (복귀 & 잔향, 80~110ms)**: 시전자가 안정적으로 정위치 복귀하고 잔여 입자가 서서히 페이드아웃.

---

## 🎥 4. 카메라 무빙 (Gen 5 다이내믹 카메라) 가이드 & 특이사항

ROGUEPot 배틀 엔진은 포켓몬 5세대(BW/B2W2) 스타일의 부드러운 다이내믹 카메라 시스템(`battleCamera.ts`)을 탑재하고 있습니다.

### 1) 카메라 타입 (`type`)
| 타입 | 용도 및 연출 설명 | 초점 위치 (Focal Point) |
|---|---|---|
| `"target"` (기본값) | 일반 물리/특수 공격기. 상대를 향해 줌인 포커싱 | 플레이어 공격 시 0.48 다이내믹 심도, 적 공격 시 플레이어 정중앙 |
| `"attacker"` | 버프기(칼춤, 성장), 상태이상 회복 등 시전자 강조 | 시전자 포켓몬 정중앙 (`280x190`) |
| `"neutral"` | 지진, 파도타기 등 전장 전체를 뒤덮는 광역기 | 중립 전장 시점 (`zoom: 1.0`) |
| `"custom"` | 공중날기, 지구던지기 등 연출별 특수 카메라 | 커스텀 좌표 함수 제공 |

### 2) 줌 배율 (`zoom`) 표준
- **`1.34 ~ 1.35` (표준/권장)**: 가장 자연스럽고 박진감 넘치는 기본 배틀 줌.
- **`1.20 ~ 1.25` (마일드 줌)**: 이펙트 반경이 넓거나 발사체 비행 궤적을 넓게 보여줘야 할 때.
- **`1.40 ~ 1.45` (클로즈업 줌)**: 파괴광선, 지구던지기 등 단발 극딜기나 필살기 연출.

### 3) ★ 핵심 특이사항: "시전 준비 시 조망 ➔ 발사 시 포커싱" (`delayUntilStep`)
> **상황**: 시전자가 제자리에서 빙글빙글 돌거나, 춤을 추거나, 기를 모으는 동안에는 **화면을 확대하지 않고 전체 전장(1.0x)을 조망**하다가,  
> 기를 모아 상대에게 발사/쇄도하는 순간(`moveStep: N`)부터 **상대방을 향해 줌인(포커싱) 후 타격**하고 싶을 때 사용합니다. (예: 꽃잎댄스)

- **설정 방법**:
  ```typescript
  export const petalDanceMove: BattleMoveAnimation = {
    // ...
    camera: { type: "target", zoom: 1.34, delayUntilStep: 5 }, // 5단계(발사)부터 줌인 포커싱!
    // ...
  };
  ```
- **엔진 동작 원리**:
  1. `moveStep < delayUntilStep` (1~4단계): 시작 시 줌인 프레임 생성을 생략하고, `cameraZoom = 1.0` 중립 시점으로 유지.
  2. `moveStep === delayUntilStep` (5단계) 진입 직전: **2프레임(50ms x 2) 부드러운 글라이드인 포커싱 자동 삽입** ("포커싱 후 발사" 실현).
  3. 이후 타격/폭발: `zoom: 1.34`로 상대방 집중 포커싱 유지.
  4. 피날레 후: 큐빅 이징 아웃(Cubic Ease-Out)으로 부드럽게 중립 `1.0x`로 자동 복귀.

### 4) ★ 카메라 관련 개발 주의점 & 사고 방지
1. **`baseFrame`의 `cameraZoom` 값**:
   - `delayUntilStep`을 사용할 경우 `baseFrame`의 `cameraZoom`을 `1.0`으로 기본 설정하세요. (사전 줌인 방지)
2. **마지막 복귀 프레임(`피날레 복귀`)의 `moveStep` 명시**:
   - 마지막 프레임에 `moveStep`을 생략하면 기본값 1로 판정되어, 카메라가 부드럽게 복귀(Glide-Out)하기 직전에 화면이 1.0x로 툭 튀는 현상이 생깁니다.
   - 반드시 마지막 프레임에도 `moveStep: 9` (또는 타격 이후 단계 번호)를 명시하세요!
3. **수동 줌아웃 프레임 작성 금지**:
   - 엔진의 `createSmoothCameraReturnFrames`가 기술 종료 시 자동으로 부드러운 줌아웃을 생성하므로, 개발자가 수동으로 줌아웃 프레임을 배열 끝에 추가할 필요가 없습니다.

---

## ⚡ 5. 필수 빌드 & 검증 명령어

작업 완료 후 항상 터미널에서 순서대로 실행하세요:

```powershell
# 1. TypeScript 컴파일 확인 (에러 0개 필수)
npm run build

# 2. 기술 단독 GIF 렌더링 테스트 (내 포켓몬 시점)
npx tsx scripts/testBattleMove.ts <기술키>

# 3. 상대 시점 GIF 렌더링 테스트 (적 포켓몬 시점)
npx tsx scripts/testBattleMove.ts <기술키> --enemy

# 4. 웹 뷰어 캐시 클리어 (즉시 반영)
curl.exe -s -X POST http://localhost:3456/api/clear-cache
```

---

## 💡 6. 문제 해결 (FAQ)

- **Q. Node에서 ERR_MODULE_NOT_FOUND 에러가 납니다.**
  - Node.js로 `src/`의 TypeScript 파일을 직접 실행하지 마세요. 개발 환경에서는 반드시 `npx tsx` 또는 `npm run dev`를 사용해야 합니다.
- **Q. 웹 뷰어에 기술이 안 뜹니다.**
  - `viewer/movesData.ts`의 `HANDCRAFTED_MAP`에 해당 기술 키가 등록되었는지 확인하세요.
- **Q. 뷰어 좌측 목록에서 4개 묶음 표시가 깨집니다.**
  - 기술 번호 `num`이 올바르게 지정되었는지 확인하세요. 번호 순서(`Math.floor((num - 1) / 4)`)에 따라 4개씩 자동으로 흰색 BORDER 박스로 묶입니다.
- **Q. 시전자가 도약하거나 회전할 때 상대방도 같이 돌거나 움직입니다.**
  - 절대 금지 규칙 2번을 확인하세요. `cOff(x, y)` 헬퍼 함수를 사용하여 시전자만 오프셋을 받고 수비자는 `{ x: 0, y: 0 }`으로 고정해야 합니다.
- **Q. 카메라가 끝나갈 때 갑자기 1.0으로 툭 튀며 끊깁니다.**
  - 마지막 프레임에 `moveStep`이 생략되어 `moveStep: 1`로 리셋된 경우입니다. 프레임에 `moveStep: 9` 등 타격 이후의 스텝 번호를 꼭 명시해주세요.
